module.exports.getRoot = (req, res) => {
  // const Users = require('../models/Users');

  // const user = new Users({
  //    email: req.body.email,
  //    password: req.body.password
  // });

  // res.json(user);
  // console.log(user);

  // user.save();
  res.send('helllo from trainings');
};
